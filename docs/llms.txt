# tosijs-timezone-picker v0.6.0

A lightweight, mobile-friendly timezone-picker web-component: an interactive SVG world map plus offset-aware autocomplete, with no timezone dataset to ship.

- Docs: https://timezones.tosijs.net
- Source: https://github.com/tonioloewald/tosijs-timezone-picker
- npm: https://www.npmjs.com/package/tosijs-timezone-picker

## Documentation

Full documentation, with live code examples, is at the links below.

Live examples run inline on the shared page by default (not sandboxed):
each has its own `preview` element, but they share one tosijs module, so
`tosi()` state singletons are shared across every example on a page. This is
deliberate (it shows tosi isolation and lets you drive demos via the global
singleton). Namespace example state with unique top-level keys so examples
don't clobber each other; the `iframe` attribute isolates DOM/CSS, not state.

## Pages

- https://timezones.tosijs.net/timezone-picker/ — <tosijs-timezone-picker>
  The timezone-picker itself: an interactive SVG world map plus offset-aware autocomplete, shipped as a tosijs blueprint.
- https://timezones.tosijs.net/components/ — components
  The custom elements tosijs-timezone-picker registers.
- https://timezones.tosijs.net/index/ — exports
  What the tosijs-timezone-picker package exports.
- https://timezones.tosijs.net/reference/ — reference
  Every export in tosijs-timezone-picker: the timezone list, the map's region data, and the package's entry points.
- https://timezones.tosijs.net/regions/ — regions
  The map's polygon data: what a Region is, how its offset is derived, and why regions can disappear.
- https://timezones.tosijs.net/timezones/ — timezones
  The Intl-derived timezone list: Timezone, timezones, localTimezone, and the name/id lookup helpers.
- https://timezones.tosijs.net/ — tosijs-timezone-picker
  A lightweight, mobile-friendly timezone-picker web-component: an interactive SVG world map plus offset-aware autocomplete, with no timezone dataset to ship.
